# Hyperion Fleet Manager API -- development / test dependencies
# Install alongside production deps:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- Testing ---
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
pytest-cov>=6.0.0,<7.0.0
httpx>=0.27.0,<1.0.0

# --- AWS mocking ---
moto[all]>=5.0.0,<6.0.0

# --- Linting & formatting ---
ruff>=0.8.0,<1.0.0
mypy>=1.13.0,<2.0.0
black>=24.10.0,<25.0.0

# --- Type stubs ---
boto3-stubs[ec2,ssm,cloudwatch]>=1.35.0
